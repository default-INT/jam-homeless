const apiUrl = "http://localhost:5000";
const apiUserInfo = apiUrl + "/user-info";
const apiMyAdverts = apiUrl + "/user-adverts";

const apiUrls = {
    apiUrl: apiUrl,
    userInfoUrl: apiUserInfo,
    apiMyAdverts
};

export default apiUrls;

